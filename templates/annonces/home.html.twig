{% extends 'base.html.twig' %}
{% block body %}

    <div class="container">
        <div class="row">
                <img src="https://static.lpnt.fr/images/2017/04/11/8203277lpw-8203313-article-jpg_4218218_660x281.jpg"
                 style="width:100%; height:auto;
                        box-shadow:-4px 6px 15px 0px rgba(0, 0, 0, 0.7);  !important"/>


                <div class="col-sm-12 pt-4"><h1><strong>Liste d'annonces</h1></strong></div>


                {% for annonce in annonces %}
                <div class="col-sm-12 col-md-4 mt-4" style="position:relative; height:470px";>
                    <h4><strong>{{annonce.titre}}</strong></h4>
                    {# <p>{{annonce.pseudo}}</p> #}
                    {# <div style="position:absolute; top:10px;"> #}
                    <img src="{{annonce.photo}}" style="width:100%; maxheight:260px !important; ">
                    {# <div><small>
                    Annonce publiée le : {{annonce.createdAt | date("d/m/Y")}} 
                                    à : {{annonce.createdAt | date("H:i")}} 
                    </small></div> #}
                    {# </div> #}

                    <h5 style="position:absolute; padding-top:20px; left:26px ;">Prix : <strong>{{annonce.prix}} euros</strong></h5>
                  <a href={{path('annonce_detail', {'id':annonce.id})}} class="btn btn-outline-dark" style="position:absolute; bottom:20px;">En savoir plus</a>
                      <br><br>
  </div>
              

                {% endfor %}
        </div>

    </div >

{% endblock %}